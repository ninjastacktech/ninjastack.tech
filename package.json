{
  "name": "ninjastack.tech",
  "version": "0.0.1",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "postcss src/styles.css -o dist/compiled.css",
    "copy": "copyfiles -u 1 src/*.{html,js} dist/",
    "build": "npm run build-css && npm run copy",
    "start": "http-server ./dist",
    "publish": "npm run build && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.2",
    "copyfiles": "^2.4.1",
    "http-server": "^14.1.0",
    "postcss": "^8.4.5",
    "postcss-cli": "^9.1.0",
    "tailwindcss": "^3.0.16"
  }
}
